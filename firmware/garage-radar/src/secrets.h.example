// Copy to secrets.h and fill in. secrets.h is git-ignored.
#pragma once

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASS "your-wifi-password"

// mDNS hostname -> the device answers at http://garage-radar.local and
// ws://garage-radar.local:81. Keep in sync with sensor.url in defaults.json.
#define DEVICE_HOSTNAME "garage-radar"
